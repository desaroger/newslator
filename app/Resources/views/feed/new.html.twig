{% extends 'base.html.twig' %}

{% block body %}
    <h2>Feed creation</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        {# Button right (cancel, save) #}
        <div class="right">
            <a href="{{ path('feed_index') }}" class="waves-effect waves-teal btn-flat">Cancel</a>
            <button class="btn waves-effect waves-light" type="submit" name="action">Save
                <input type="submit" value="" class="hide-hack"/>
            </button>
        </div>

    {{ form_end(form) }}

{% endblock %}